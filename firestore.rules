rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all direct client-side reads and writes.
    // All access must go through the authenticated Next.js server-side Route Handlers (Firebase Admin SDK).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
